<div class="mt-4">
    <div class="row ">
        {{#each courses}}
        <div class="col-md-4 mb-4">
            <div class="card">
                <a href="/course/{{this.slug}}"><img src="{{this.image}}" class="card-img-top" alt="...">
                </a>
                <div class="card-body">
                    <a href="/course/{{this.slug}}">
                        <h5 class="card-title">{{this.name}}</h5>
                    </a>
                    <p class="card-text ">{{this.description}}</p>
                    <a href="#" class="btn btn-primary">Mua ngay</a>
                </div>
            </div>
        </div>
        {{/each}}
    </div>



</div>
</div>